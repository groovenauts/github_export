require 'spec_helper'

describe GithubExport do
  it 'has a version number' do
    expect(GithubExport::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
